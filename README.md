Present code adresses:

    • Split the data into training and testing sets.
    • Scale the features.
    • Create a logistic regression model.
    • Create a random forest model.
     Evaluate the models
